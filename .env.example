# Environment configuration sample for Blog Portal (CodeIgniter 3)

# CodeIgniter environment
CI_ENV=development

# Application
# If using PHP built-in server on port 8080, keep trailing slash
BASE_URL=http://localhost:8080/blog-portal/
# 32+ char random string used as CI encryption key
APP_KEY=change_this_to_a_random_32_char_string

# JWT
# Long random secret for HS256 signing
JWT_SECRET=change_this_to_a_long_random_string
# Token TTL in seconds (e.g., 1 hour)
JWT_TTL=3600
# Refresh token TTL (e.g., 14 days in seconds)
REFRESH_TTL=1209600

# Database (MySQL)
DB_HOST=127.0.0.1
DB_NAME=blog_ci
DB_USER=root
DB_PASS=

# Pixabay (server-side proxy uses this; do not expose in frontend)
PIXABAY_API_KEY=your_pixabay_api_key_here

# Pagination default
PAGINATION_PER_PAGE=10
